function y = tutorial_img_display(i,x,mappy_map)
%tutorial_img_display
%  Simply takes in a value for the grayscale and a 2d array of pixels and
%  outputs an image.
figure(i);image(x); colormap(mappy_map);colorbar;

y = 0;
end

